// COS配置模板 - 提交到git仓库，不包含敏感信息
const cosConfig = {
  SecretId: 'YOUR_SECRET_ID',      // 替换为您的SecretId
  SecretKey: 'YOUR_SECRET_KEY',    // 替换为您的SecretKey
  Bucket: 'your-bucket-name', // 替换为您的存储桶名称
  Region: 'your-region',      // 替换为您的存储桶所在地域
};

export default cosConfig; 